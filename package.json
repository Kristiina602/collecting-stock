{
  "name": "collecting-stock",
  "version": "1.0.0",
  "description": "A web application for tracking and managing berry and mushroom collections",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm run preview\""
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "berries",
    "mushrooms",
    "collection",
    "tracking",
    "react",
    "nodejs",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
